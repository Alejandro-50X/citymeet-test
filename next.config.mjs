/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                protocol: 'https',
                hostname: 'neucezwnjijackkaatxk.supabase.co'
            }
        ]
    }
};

export default nextConfig;
